<% YAML.load_file(Rails.root.join("db/seeds/header_aliases.yml")).each do |field, patterns| %>
<% patterns.each do |pattern| %>
<%= field %>_<%= pattern.parameterize(separator: "_") %>:
  field: <%= field %>
  pattern: <%= pattern %>
<% end %>
<% end %>
